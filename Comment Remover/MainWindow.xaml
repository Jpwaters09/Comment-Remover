<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Comment_Remover.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Comment_Remover"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Comment Remover">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TitleBar x:Name="TitleBar" Grid.Row="0" Title="Comment Remover">
            <TitleBar.LeftHeader>
                <ImageIcon Height="16" Margin="16, 0, 0, 0" Source="ms-appx:///Images/Comment Remover Logo.png"/>
            </TitleBar.LeftHeader>
        </TitleBar>

        <NavigationView Grid.Row="1" PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" SelectionChanged="NavBarSelectionChanged" x:Name="NavBar">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" ToolTipService.ToolTip="Home" Content="Home" Tag="HomePage" FocusVisualPrimaryBrush="Transparent" FocusVisualSecondaryBrush="Transparent"/>
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
